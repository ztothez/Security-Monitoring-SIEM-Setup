on_http_request:
  - actions:
      - type: oauth
        config:
          provider: github
          client_id: <redacted>  # Replace with your GitHub OAuth Client ID
          client_secret: <redacted>  # Replace with your GitHub OAuth Client Secret
          scopes: ["read:user"]  # Required for authentication
          auth_id: <redacted>  # Restrict to your GitHub username